# Keep build context small: only what the Dockerfile actually copies.
*
!packages/
!runtime/

# Inside packages/, only tarballs.
packages/*
!packages/*.tar.gz

# Inside runtime/, only what we COPY in.
runtime/*
!runtime/Dockerfile
!runtime/fe-entrypoint.sh
!runtime/be-entrypoint.sh
